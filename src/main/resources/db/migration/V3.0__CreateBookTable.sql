CREATE TABLE book(id SERIAL PRIMARY KEY, name VARCHAR (300) NOT NULL, coverPicture VARCHAR (300), releaseDate DATE, author VARCHAR (300), pricing_category VARCHAR(20) REFERENCES PRICE_INFO(PRICING_CATEGORY));